{
  "name": "subgraph-grants-contract",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ subgraph-grants-contract",
    "create-local": "graph create --node http://localhost:8020/ subgraph-grants-contract",
    "remove-local": "graph remove --node http://localhost:8020/ subgraph-grants-contract",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 subgraph-grants-contract"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.2",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
