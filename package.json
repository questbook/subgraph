{
  "name": "@questbook/subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "test": "graph test",
    "prepare-rinkeby": "mustache config/rinkeby.json template.subgraph.yaml > subgraph.yaml",
    "create-subgraph-rinkeby": "graph create --node http://the-graph.questbook.app:8020/ qb-subgraph",
    "deploy-rinkeby": "yarn prepare-rinkeby && graph deploy --node http://the-graph.questbook.app:8020/ --ipfs https://ipfs.infura.io:5001 qb-subgraph",
    "prepare-harmonytest": "mustache config/harmonytest.json template.subgraph.yaml > subgraph.yaml",
    "create-subgraph-harmonytest": "graph create --node http://the-graph.questbook.app:8020/ qb-subgraph-harmonytest",
    "deploy-harmonytest": "yarn prepare-harmonytest && graph deploy --node http://the-graph.questbook.app:8020/ --ipfs https://api.thegraph.com/ipfs/ qb-subgraph-harmonytest",
    "remove-local": "graph remove --node http://localhost:8020/ qb-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs https://api.thegraph.com/ipfs/ qb-subgraph",
    "create-subgraph-local": "graph create --node http://localhost:8020/ --ipfs https://ipfs.infura.io:5001 qb-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.2",
    "@graphprotocol/graph-ts": "0.24.1"
  },
  "devDependencies": {
    "matchstick-as": "^0.2.3",
    "mustache": "^4.2.0"
  }
}
